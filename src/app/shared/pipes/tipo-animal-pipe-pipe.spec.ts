import { TipoAnimalPipePipe } from './tipo-animal-pipe-pipe';

describe('TipoAnimalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TipoAnimalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
