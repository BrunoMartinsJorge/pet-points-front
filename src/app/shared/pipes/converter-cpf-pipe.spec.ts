import { ConverterCpfPipe } from './converter-cpf-pipe';

describe('ConverterCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverterCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
