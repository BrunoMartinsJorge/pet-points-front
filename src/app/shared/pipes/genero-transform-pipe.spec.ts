import { GeneroTransformPipe } from './genero-transform-pipe';

describe('GeneroTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneroTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
